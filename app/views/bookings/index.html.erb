<h1>Bookings#index</h1>
<p>Find me in app/views/bookings/index.html.erb</p>

<div class="container">
  <div class="row">
    <div class="col">
      <% unless @bookings.nil? %>
        <% @bookings.each do |booking| %>
          <div>
            <%= booking.id %>
          </div>
        <% end %>
      <% end %>
    </div>
  </div>
</div>
