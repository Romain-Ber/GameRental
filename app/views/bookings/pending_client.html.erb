<%= render "pages/navbar"%>
<div class="container">
  <h1 class="animate-charcter" style="font-size: 30px;">My Pending Clients</h1>
  <div class="row">
    <div class="col">
      <% unless @bookings.nil? %>
        <% @bookings.each do |booking| %>
          <%= link_to booking_path(booking, query_type: "owner"), class:"text-decoration-none" do %>
            <div class="booking-card">
              <p>Game: <%= booking.game.name %>
              <p>Status: <%= booking.status %></p>
              <p>Price: <%= booking.game.price %>€</p>
            </div>
          <% end %>
        <% end %>
      <% end %>
    </div>
  </div>
</div>
<%= render "pages/footer"%>
